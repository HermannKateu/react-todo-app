import {FormWrapper} from "./components/FormWrapper";

function App() {
  return (
    <div className="bg-gradient-to-r from-sky-500 to-indigo-500 h-screen flex items-center justify-center">
      <FormWrapper />
    </div>
  );
}

export default App;
